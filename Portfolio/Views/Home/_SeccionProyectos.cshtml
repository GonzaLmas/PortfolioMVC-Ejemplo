@model IEnumerable<ProyectoViewModel>

@if (Model is not null)
{
    <div class="text-center" style="padding-top: 6rem">
        <h1>Aquuí puedes ver mis proyectos</h1>
        <p>Aquí puedes ver mis trabajos más recientes</p>
    </div>

    <partial name="_ListadoProyectos" model="Model"/>
}